# Placeholder — requires Oracle VM deploy for static IP; see docs/plans/
#
# Once deployed on Oracle Cloud with a static IP (whitelisted in Binance API),
# this workflow can be enabled to run the position monitor on a cron schedule.
#
# name: Position Monitor
#
# on:
#   schedule:
#     - cron: '*/15 * * * *'   # every 15 minutes
#   workflow_dispatch:           # manual trigger
#
# jobs:
#   monitor:
#     runs-on: self-hosted       # Oracle VM runner with static IP
#
#     steps:
#       - name: Checkout repo
#         uses: actions/checkout@v4
#
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.11'
#
#       - name: Install Poetry
#         run: curl -sSL https://install.python-poetry.org | python3 -
#
#       - name: Install dependencies
#         run: poetry install --no-root
#
#       - name: Run position monitor
#         env:
#           BINANCE_API_KEY: ${{ secrets.BINANCE_API_KEY }}
#           BINANCE_API_SECRET: ${{ secrets.BINANCE_API_SECRET }}
#           TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
#           TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
#         run: poetry run python buibui.py monitor position --telegram
