version: 2
updates:
  # Poetry project: Dependabot uses "pip" ecosystem to track pyproject.toml / poetry.lock
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "weekly"
    allow:
      - dependency-type: "direct" # Only update direct dependencies
    open-pull-requests-limit: 5
